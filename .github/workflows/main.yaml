# Do not edit, generated automatically
# by the create-container.py script.
# Last updated on: 2022-09-30T13:46:35-07:00

name: tpprwr
on:
  push:
    tags:
      - 'build-*'

jobs:
    build-container:
      runs-on: ubuntu-latest
      steps:
        - name: checkout
          uses: actions/checkout@v2
        - name: setup python
          uses: actions/setup-python@v2
          with:
            python-version: 3.x
        - name: install dependencies
          run: pip install pyyaml docker jinja2
        - name: create yaml file
          run: python create-container.py -r fpoitevi -t ana -v 4.4.10 -o ana-4.4.10.yml -b yaml/psana2/env_create.yaml
        - name: build container
          run: docker build --build-arg inputyaml=ana-4.4.10.yml --build-arg psana_version=ana:4.4.10 --tag fpoitevi/ana:4.4.10 docker
        - name: login to DockerHub
          uses: docker/login-action@v1
          with:
            username: ${{ secrets.DOCKERHUB_FRED_USERNAME }}
            password: ${{ secrets.DOCKERHUB_FRED_TOKEN }}
        - name: upload container
          run: docker push fpoitevi/ana:4.4.10